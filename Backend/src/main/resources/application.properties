spring.application.name=RCPT
# Disable DataSource auto-configuration for local Docker Compose (no DB configured)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
